mutation CompleteDraftOrder($id: ID!) {
    draftOrderComplete(id: $id) {
        draftOrder {
            id
            name
            invoiceUrl
        }
        userErrors {
            field
            message
        }
    }
}
